queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions: []
    merge_method: merge
    commit_message_template: 'GROUP={{ [title]|groupby("__class__")|map(attribute="grouper")|list }}'
