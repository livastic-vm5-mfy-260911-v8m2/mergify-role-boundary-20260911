queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions: []
    merge_method: merge
    commit_message_template: 'SAFE={{ [number]|map(attribute="real")|list }} DUNDER={{ [title]|map(attribute="__class__")|list }}'
