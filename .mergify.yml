pull_request_rules:
  - name: vm5 vitest delimiter collision protected merge
    conditions:
      - title = VM5 Vitest delimiter protected collision 260917
      - check-success = @github-actions/vitest-collision-gate
    actions:
      merge:
        method: merge

queue_rules:
  - name: default
    queue_conditions: []
