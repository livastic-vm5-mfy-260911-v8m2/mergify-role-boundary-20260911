pull_request_rules:
  - name: vm5 stale requested-reviewer natural-event probe
    conditions:
      - title = VM5_REVIEW_REQUESTED_READY
      - -draft
      - review-requested = livastic-vm5-secondary-20260911
    actions:
      merge:
        method: squash

