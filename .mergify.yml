pull_request_rules:
  - name: vm5 issue-comment trusted gate merge proof
    conditions:
      - number = 24
      - check-success = vm5-issue-comment-trusted-gate
    actions:
      merge:
        method: merge

queue_rules:
  - name: default
    queue_conditions: []
