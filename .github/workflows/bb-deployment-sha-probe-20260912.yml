name: VM5 Deployment SHA Probe
on:
  workflow_dispatch:

jobs:
  deploy:
    name: VM5_DEPLOYMENT_SHA_PROBE
    runs-on: ubuntu-latest
    environment: vm5-deployment-sha-probe-20260912
    steps:
      - run: echo VM5_DEPLOYMENT_SHA_PROBE
