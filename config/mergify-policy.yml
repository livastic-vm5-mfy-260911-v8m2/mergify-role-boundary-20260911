pull_request_rules:
  - name: vm5 symlink config benign authority canary
    conditions:
      - title = VM5 never match symlink benign authority 260917
    actions:
      comment:
        message: vm5 symlink authority canary
  - name: vm5 symlink PR-only target mutation canary
    conditions:
      - title = VM5 never match symlink PR target 260917
    actions:
      comment:
        message: vm5 symlink PR target mutation canary

queue_rules:
  - name: default
    queue_conditions: []
